# Base URL of the Express backend (chatbot, tours, auth, payments, ...)
# The frontend reads this via src/utils/API_Port.js.
REACT_APP_API_URL=http://localhost:3001

# For local development with all three services:
# - Frontend: http://localhost:3000
# - Backend (Express API): http://localhost:3001
# - AI Agent (FastAPI): http://localhost:8000
